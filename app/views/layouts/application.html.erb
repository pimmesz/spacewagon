<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>SpaceWagon</title>
  <%= csrf_meta_tags %>
  <%= action_cable_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= stylesheet_pack_tag 'application' %>
  <link href="assets/favicon.png" rel="shortcut icon" type="image/x-icon" />

   <!-- Uncomment if you import CSS in app/javascript/packs/application.js, Steef uncommended this because he needs it for his form-->

</head>
<body>
  <% unless notice.nil? %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= render 'shared/flashes' %>
  </div>
<% end %>
  <%= render 'shared/navbar' %>
  <% if notice.nil? %>
  <div class="body-margin">
    <%= yield %>
  </div>
  <% else %>
  <div>
    <%= yield %>
  </div>
  <% end %>
  <script src="https://unpkg.com/scrollreveal/dist/scrollreveal.min.js"></script>
  <%= javascript_include_tag 'application' %>
  <%= javascript_pack_tag 'application' %>
  <%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?libraries=places&key=#{ENV['GOOGLE_API_BROWSER_KEY']}" %>

  <%= javascript_pack_tag "map" %>
  <%= render 'shared/footer' %>
</body>
</html>
